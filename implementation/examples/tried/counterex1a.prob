var n,b,x,y;
while x>=0 and y>=0 and y<=n do
	if b>=0 and b<=0 then
		y := y+1;
		if * then
			b := 1
		else
			skip
		fi
	else
		y := y-1;
		if * then
			x := x-1;
			b := 0
		else
			skip
		fi
	fi
od