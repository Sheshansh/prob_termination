var i,j,n;
j:=0;
i:=n;
if n>=1 then
	if * then
		j:=j+1;
		if j>=n then j:=0 else skip fi
	else
		j:=j-1;
		if j<=0 then j:=0 else skip fi
	fi;
	i:=i-1;
	while i>0 do
		if * then
			j:=j+1;
			if j>=n then j:=0 else skip fi
		else
			j:=j-1;
			if j<=0 then j:=0 else skip fi
		fi;
		i:=i-1
	od
else skip fi