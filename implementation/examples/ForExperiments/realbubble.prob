var length,i, j,test;
i := length - 1;
while i > 0 and test >=1 and test<=1 do

  test:=0;
  j := 0;
  while j < i
  do
    if * then
    
      skip;
      test:=1
    else skip fi;
    j:=j+1
  od;
  i:=i-1
od