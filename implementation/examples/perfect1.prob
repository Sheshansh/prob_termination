var x, y1, y2, y3;

if x <= 1 then skip else

  y2:=x;
  y1 := x-1; 
  while y1>=1 do
if prob(0.5) then

    while y2 >= y1 do
if prob(0.5) then y2 := y2 - y1 else skip fi
od;
    y2 := x;
    y1:=y1-1

  else skip fi
od

fi
