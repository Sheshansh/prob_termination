var array_size, i, j, min, temp;
i := 0;
while i < array_size-1 do
if prob(0.5) then
  min := i;
  j := i+1;
  while j < array_size do
if prob(0.5) then
    if * then
      min := j
    else skip fi;
  	j:=j+1
  else skip fi
od;
  skip;
  i:=i+1
else skip fi
od