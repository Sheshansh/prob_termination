var x,y,z,n1,n2,c1,c2;
c1 := 0;
if n2 <= 2*y then
	skip
else
	if n2 <= z+y then
		skip
	else
		if z>=0 and x>=0 and y>=0 and n1>=0 and n2>=0 then
			while n1-1 >= 0 do
			if prob(0.1) then
				n1 := n1-1;
				c2 := 0;
				c1 := c1+1;
				while n2 > 0 do
					n2 := n2-1;
					z := y;
					c2 := c2+1
				od;
				z := z+y;
				n2 := z
			else skip fi
			od;
			n1 := n1-1
		else
			skip
		fi
	fi
fi
