var i,j,n;
j:= [-2,2]+0;
i:= [-2,2]+n;
if n>=1 then
	if * then
		j:= [-2,2]+j+1;
		if j>=n then j:= [-2,2]+0 else skip fi
	else
		j:= [-2,2]+j-1;
		if j<=-1*n then j:= [-2,2]+0 else skip fi
	fi;
	i:= [-2,2]+i-1;
	while i>0 do
if prob(0.5) then
		if * then
			j:= [-2,2]+j+1;
			if j>=n then j:= [-2,2]+0 else skip fi
		else
			j:= [-2,2]+j-1;
			if j<=-1*n then j:= [-2,2]+0 else skip fi
		fi;
		i:= [-2,2]+i-1
	else skip fi
od
else skip fi