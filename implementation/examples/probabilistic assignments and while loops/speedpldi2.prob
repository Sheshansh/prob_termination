var v1, v2, n, m;
if n>=0 and m>=1 then
	v1 := [-1,1]+ n;
	v2 := [-1,1]+ 0;
	while v1 >=1 do
if prob(0.5) then
		if v2<m then
			v2:= [-1,1]+v2+1;
			v1:= [-1,1]+v1-1
		else 
			v2 := [-1,1]+ 0
		fi
	  else skip fi
od
else skip fi
