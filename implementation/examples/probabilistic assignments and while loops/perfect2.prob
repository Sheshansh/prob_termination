var x, y1, y2, y3;

if x <= 0 then skip else

  y2:= [-1,1]+x;
  y1 := [-1,1]+ x-1; 
  while y1>=2 do
if prob(0.5) then

    y1:= [-1,1]+y1-1;
    while y2 >= y1 do
if prob(0.5) then y2 := [-1,1]+ y2 - y1 else skip fi
od;
    y2 := [-1,1]+ x

  else skip fi
od

fi
