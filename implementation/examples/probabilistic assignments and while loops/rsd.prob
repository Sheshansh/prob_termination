var r,da,db,temp,c1;
c1:= [-2,2]+0;
if r>=0 then
  da := [-2,2]+ 2*r;
  db := [-2,2]+ 2*r;
  while da >=r do
if prob(0.5) then
    if * then
      da:= [-2,2]+da-1
    else
      temp := [-2,2]+ da;
      da := [-2,2]+ db - 1;
      db := [-2,2]+ temp
    fi;
    c1:= [-2,2]+c1+1
  else skip fi
od
else skip fi